package ch02.lecture.p02type;

public class C01Type {
	public static void main(String[] args) {
		// java의 data type(자료형)
		// 1. 기본타입(primitive type)
		// 8개
		// (정수) byte, short, int, long
		// (문자/정수) char
		// (실수) float, double
		// (논리) boolean
		
		
		// 2. 참조타입(reference type) - 5장에서 배울 예정
		// 기본 타입 외 모두 다 (무한히 많음)
		
		// 변수 선언 시 사용된 타입의 값만 변수에 할당(대입) 가능
		int a;
		a = 222;
//		a = "222"; // X
	}
}
